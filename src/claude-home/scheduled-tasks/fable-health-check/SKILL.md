---
name: fable-health-check
description: Monthly read-only audit of the fable system — preflight, pair consistency, hook integrity + behavior, adoption evidence with conversion, kit freshness, memory scope integrity
---

You are running the monthly fable-system health check on this Windows machine. The fable system is a set of coupled instruction artifacts that make Claude Code sessions run frontier-grade process: `~\.claude\CLAUDE.md` (global discipline file), `~\.claude\skills\fable-mode\SKILL.md` (operating-method skill, with `references\gold-standards.md`), hooks inside `~\.claude\settings.json` (a SessionStart standing-order echo, a UserPromptSubmit prompt-scoped classifier, and a SubagentStart additionalContext injection), `~\.claude\skills\refresh-kit\SKILL.md`, `~\.claude\skills\invest-research\SKILL.md`, `~\.claude\skills\organize\SKILL.md`, the project memory dir under `~\.claude\projects\<project-slug>\memory\`, and the kit repo — the fable project folder containing `src\` and `FableSetup.exe`, wherever it is checked out (`~` = the user's home).

This task is STRICTLY READ-ONLY over the live system files: edit nothing, fix nothing, rebuild nothing — report only. Governance (per CLAUDE.md's automation clause): output channel = this scheduled run's report surface, nowhere else; escalation = report-only, fixes are proposed to the user, never self-applied; stop condition = an audit impossible for the 2nd consecutive month stops being re-attempted and shrinks to one line surfacing the failure until the user intervenes — the schedule itself is user-owned, never self-disable or self-reschedule.

Run these audits and report findings-first, each claim labeled verified (command + decisive output line pasted) or inferred, gaps named rather than silently skipped:

0. PREFLIGHT — probe the instruments and paths every audit depends on: bash.exe at `C:\Program Files\Git\bin\bash.exe` (else Get-Command bash), Get-Command node, pwsh, and git (audit 4(g)'s private-repo checks need git), and Test-Path on every live file named above plus the kit repo and the memory dir. Any miss: never error out mid-audit and never mark a dependent audit PASS — run what remains, report each blocked audit as ATTENTION "unverifiable — <instrument/path> missing", and lead the report with the miss (bash missing = hooks fall back to PowerShell per the platform hooks doc: SessionStart and SubagentStart survive, the bash-syntax classifier dies noisily — lead with the classifier loss, verified not assumed). Project folder or memory dir missing while `~\.claude` is intact is the renamed-project signature: memory lives under the old path slug — say so, recommend migration, report-only.

1. PAIR CONSISTENCY — Read CLAUDE.md and fable-mode SKILL.md in full. Verify the shared thresholds still agree in both files: 2-attempt cap with a 3rd attempt only on new evidence; 1 fresh-eyes reviewer default and 2 with different lenses for High-risk; >=3 pre-declared runs to claim flakiness; Ledger threshold of 3+ dependent edits or commands, or 3+ files; and the stricter-rule-wins deference clause; the intent-gate artifact-authority order (user > spec > tests > code) and the `INTENT: code does X / check expects Y / spec says Z` artifact present in BOTH files; and CLAUDE.md's 0–20 "Stating calibrated confidence" section present. Also verify the memory-scope discipline is intact: CLAUDE.md carries the three memory gates (write / recall / overrides & hygiene) and MEMORY.md still carries the sectioned protocol header (Global / Topics / Threads). Report any mismatch with exact quotes and line numbers from both files.

2. HOOK INTEGRITY & BEHAVIOR — Parse `~\.claude\settings.json` with PowerShell ConvertFrom-Json. Confirm all three hooks exist (SessionStart command beginning "echo 'Standing order", a UserPromptSubmit command containing grep -qiE, and a SubagentStart command). Extract the UserPromptSubmit command from the PARSED JSON and pipe-test it via Git Bash (never hand-transcribe it — escape layers collapse \\b into backspace characters that silently kill regex arms):
   - stdin {"prompt":"refactor the auth module"} must print the classifier reminder and exit 0;
   - stdin {"prompt":"add a PreToolUse hook to settings.json"} must print (config-stem coverage);
   - stdin {"prompt":"deep dive on ASTS"} must print (domain-stem coverage);
   - stdin {"prompt":"run a tournament to pick the design"} must print (v6-stem coverage);
   - stdin {"prompt":"improve the error handling in parser.js"} must print (v7-stem coverage);
   - stdin {"prompt":"restore prod from the backup"} must print (v8-stem coverage — the r26 arms: tls/ssl, inject, restore, backup, chmod, reset --hard);
   - stdin {"prompt":"whats the max dose of amoxicillin for a 6 year old"} must print (v9-stem coverage — the r31 clinical arms: dose/dosage/clinical/patient/diagnosis-forms/contraindic/prescri/symptom/medicat/dentist/dental; "diagnostics" deliberately does NOT fire — r32 tuning);
   - stdin {"prompt":"fix typo in readme"} must print nothing and exit 0;
   - stdin {"cwd":"C:/repos/auth-service","prompt":"hi"} must print nothing and exit 0 (prompt-scoping);
   - stdin {"cwd":"C:/repos/auth-service","prompt":""} must print nothing and exit 0 (empty-prompt sentinel);
   - stdin not-json{{ must print nothing and exit 0 (malformed payload → sentinel);
   - the auth-service "hi" stdin re-run with node stripped from PATH (export PATH=/usr/bin:/bin) must print exactly one line containing DEGRADED, no classifier reminder, and exit 0 (fail-loud degrade — a silent whole-envelope fallback is the pre-v5 regression).
   Also run the extracted SubagentStart command with any stdin: its stdout must strict-parse as JSON with hookSpecificOutput.hookEventName = "SubagentStart", and its additionalContext must carry the INTENT artifact ("INTENT: code does").

3. ADOPTION EVIDENCE — Under `~\.claude\projects\`, take .jsonl transcript files modified in the last 31 days, newest first, capped at 30 files (declare the cap and the total candidate count). EXCLUDE the transcript files belonging to the session running this audit before counting — self-referential hits are contamination, not adoption. Across the sample count: (a) files containing "Prompt classifier (user hook)" — classifier fired; (b) files matching the invocation regex "skill": ?"fable-mode" — skill actually INVOKED — plus, as a separate labeled column, files containing the body sentinel "frontier operating patterns" — content loaded. NEVER count the bare string "fable-mode" as adoption: the hooks and the skills list inject it into transcripts that never loaded the skill. (c) files showing subagent spawns with an explicit strongest-tier model override — if the platform's frontier tier has been renamed or retired, substitute the current strongest tier's identifier and note the substitution. Sessions already running the strongest tier need no override — check each sampled session's own model and exclude frontier-tier parents from count (c)'s denominator before flagging. (d) CONVERSION — of the files counted in (a), how many also invoked per (b): report the fired→invoked conversion ratio. The PreToolUse-gate recommendation keys on LOW conversion with (a) high, or on (c) near zero among below-frontier parents. If (a) is 0 across a sample containing classifier-shaped prompts WHILE audit 2's pipe-tests pass, the harness may have stopped firing hooks: report ATTENTION hook-liveness — confirm in a live session (claude --debug shows hook execution) — dead hooks and insufficient reminders are different diagnoses with different fixes. (e) EVIDENCE FIDELITY — take the 5 most recent sampled files containing a "verified:" label; for each, extract one quoted decisive line from a final assistant report, normalize whitespace, and search the SAME transcript for a >=20-char substring of it inside an earlier tool-output entry; no match = ATTENTION possible fabricated evidence (quote the line and file). Declare the cap (5); truncation and reformatting can false-flag — this ATTENTION is a lead for manual review, never a verdict. Report all counts as ratios over the sample with one or two quoted examples.

4. KIT FRESHNESS & COMPLETENESS — In the kit repo: (a) hash-compare every live file the kit stages against its `src\` counterpart (CLAUDE.md ↔ `src\claude-home\CLAUDE.md`, fable-mode SKILL.md + references, refresh-kit, invest-research, and organize SKILL.md, the scheduled-task SKILL.md). settings.json is the one EXCEPTION — compare it on DISCIPLINE content only (the three hooks + any non-personal keys), EXCLUDING the personal-overlay keys `theme`/`tui`/`autoUpdatesChannel`/`skipWorkflowUsageWarning` and key ordering: a raw hash always differs on the live `tui` overlay and that is NOT drift (the amplification regression gate's config-drift rung uses this same carve-out). Any OTHER live file hash-different from src is DRIFT → recommend the user run /refresh-kit; personal-overlay differences are EXPECTED where src ships a genericized template (e.g. invest-research's profile line) — flag only unexpected drift. A settings.json discipline-mismatch is triaged PER KEY, not blanket-flagged: a machine-local/personal top-level key beyond the four (e.g. `permissions`, `model`, `statusLine`, `env`, `feedbackSurveyState` — several are written by other skills) is an expected live overlay → note it and recommend extending the four-key carve-out in all three carriers (this file, refresh-kit, the regression gate), NOT /refresh-kit — EXCEPT a security-relevant key (`permissions`, `env`, `apiKeyHelper` and the like) gets ATTENTION with its value quoted, not a quiet note, since a widened allow-list or a PATH-shadowing env can change what runs; only a changed hook / matcher / timeout or a genuine discipline key is real DRIFT → recommend /refresh-kit. (b) the built exe — `EXE\FableSetup.exe` in the container beside the repo (three-folder layout), else `FableSetup.exe` at the repo root — exists with LastWriteTime >= the newest `src\` file's (a stale exe is a stale kit). (c) Manifest spot-check: pick 3 files listed in `src\kit-manifest.json`, recompute SHA-256, compare — any mismatch = stale manifest, ATTENTION. (d) No `fable-restore-kit-*.zip` anywhere in the repo (zip model retired at r19) — one found = superseded artifact; recommend deletion only after the exe is confirmed working. (e) No literal `.claude\` directory inside the repo (the payload is `claude-home\` precisely so cloned repos don't register duplicate scoped skills; a `.claude\` dir reintroduces the trap) and no extracted kit trees. (f) Memory DR: restate that project memory is NOT in the kit (public-kit policy, r19); since r20 refresh-kit mirrors it into the container's `private\memory\` — verify that mirror exists and is no older than the newest live memory file; the off-machine copy of `private\` and the repo is MANUAL by the owner's standing choice — always restate it (since r28 `private\tools\backup-private.ps1` makes the encrypted off-machine copy a one-command job; restate that too). (g) Container layout (where the three-folder layout exists — the repo's parent holds a container CLAUDE.md): the parent holds exactly the working folder, `private\`, and `EXE\` (plus the container CLAUDE.md and, where the harness created one, a `.claude\` project-config dir — its EXISTENCE is expected/exempt, but LIST its contents and ATTENTION-flag any `settings*.json`, `hooks\`, or other executable config (`commands\`, `skills\`, `agents\`) inside it: agent config placed there executes for sessions opened at the container root, so the carve-out exempts the dir's presence, never unaudited hooks within it); `EXE\` holds exactly one exe; `private\` may keep its own LOCAL-ONLY git repo (r24 policy) — if `private\.git` exists verify (i) zero remotes (`git -C <private> remote` → empty output), (ii) a `pre-push` hook under `private\.git\hooks\` that ACTUALLY refuses (grep it for `exit 1` / `REFUSED` — presence alone is not efficacy), and (iii) `git -C <private> config core.hooksPath` UNSET (a redirected hooksPath silently disarms the in-repo hook); a configured remote, a missing or no-op hook, or a set hooksPath = ATTENTION (leak path), and no nested `.git` elsewhere under `private\`; `private\` appears nowhere inside the repo; anything else at the container root = ATTENTION, recommend /organize.

5. MEMORY SCOPE INTEGRITY — For each *.md except MEMORY.md in the memory dir: verify (i) a scope: value exists, top-level or under metadata: (the harness may relocate it — either address counts); (ii) the description's [global]/[topic:x]/[thread:...] prefix agrees with that scope; (iii) exactly one MEMORY.md index line references the file, sitting in the section matching its scope, with no index lines outside the three sections; (iv) every index link resolves to an existing file. Any disagreement, missing carrier, unindexed file, or orphan line = ATTENTION naming the file and the observed carrier values; narrowest scope governs until re-filed.

Output: a compact report (roughly 40 lines max), findings first, then one PASS/ATTENTION verdict per audit, then any recommendations. Do not make changes of any kind.