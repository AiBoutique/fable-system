{
  "_comment": "User-scope MCP servers to register at install time. Populate from your own machine with: node tools\\export-mcp-template.js %USERPROFILE%\\.claude.json mcp-servers.template.json (secrets are redacted to <<SET-ME>>, home paths tokenized to <<HOME>>). Ships empty in the public kit.",
  "mcpServers": {}
}
