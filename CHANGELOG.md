# Changelog

The fable system's version log, newest last. Through r18 the kit shipped as a zip (`fable-restore-kit-<date>-rN.zip`); since r19 the repo itself is the kit and `FableSetup.exe` is the artifact.

- 2026-07-02 → 2026-07-09 (through r10) — system built and hardened across ten adversarially-reviewed rounds: CLAUDE.md v2→v4, fable-mode skill + gold-standards exemplars, three hooks, monthly health check (v2, audits 0–5), refresh-kit + invest-research skills, one-click installer with MCP/plugin coverage, 67-assertion self-test on every refresh, signature-gated superseded-hook replacement, secret-scan attestation + CRC full-read + prior-set coverage gates. Round-by-round history lives in session transcripts (log compacted 2026-07-09 at owner request; prior compaction at r8).
- 2026-07-09 r11 — CLAUDE.md v4→v5 via 4-axis agentic web research + a 5-candidate/5-red-team/3-blind-judge tournament (SUBTRACT won 3–0; weighted totals computed in code). Net −5 lines (209→204), additions funded by cuts: doubt/stuck/innovation router in Reasoning (route to new external evidence or independent judgment; only new external evidence can license a 3rd attempt; at-cap preserve/report/escalate duty explicit); Tournament executable-criterion clause with judge floor; same-tier fresh-context review fallback; orient-first step in the Investigation playbook; analogy-demotion clause in debugging; Gate item 7 anti-self-re-critique echo; second-order tracing consolidated into the widened Ground Truth pre-change search. Two-lens fresh-eyes review: 1 major fixed + 3 minors hardened.
- 2026-07-09 r12 — classifier v6: UserPromptSubmit stems +tournament/innovat/agentic, closing the innovation-prompt gap found by v5's review (verified by live pipe tests). Changelog compacted to current state at owner request.
- 2026-07-09 r13 — installer gate hardening after the first real-world misfire (source-copy INSTALL.bat run at the project root): the missing-manifest branch now detects a kit zip beside the script and prints paste-proof extract-and-run guidance. Both branches execution-tested on Windows PowerShell 5.1 in sandboxes; two-lens fresh-eyes review.
- 2026-07-09 r14 — installer self-service: the missing-manifest branch OFFERS ([Y/n], default Y) to extract the newest kit zip to %TEMP% and continue installing from there — pre-cleaned extraction dir, corrupt/folder-rooted zips fall back to guidance, decline = exit 2, -Unattended keeps the hard abort, child verdict propagates. Millisecond transcript stamps. Five-branch execution battery + two-lens review.
- 2026-07-11 r15 — full-system defect-and-enhancement round: three independent auditors + owner-run execution audits; 50 findings adjudicated, ~45 fixed. The MCP-template redactor hardened against inline --flag=value credentials, in-URL-path tokens, query/fragment content in any order, full userinfo, --header values, -e NAME=VALUE env args, digit-suffixed cred names — while never rewriting hostnames or inline scripts; every leak and false-positive execution-proven first, pinned by 15 new assertions (self-test 67→84). Classifier v6→v7 (+improve/enhance stems, pipe-tested). Installer MAX_PATH guard measures the actual longest kit memory filename. refresh-kit: memory updates before staging; walkthrough updates mandated with the README. Health-check audit 4 moved to hash-equality freshness. Coupled pair: 24 wording-level defects fixed, no threshold changes. Two-lens fresh-eyes review on the full diff.
- 2026-07-11 r16 — install-from-anywhere self-service, born from the third real-world source-tree misfire: ANY integrity failure with a kit zip beside the script names the newest archive and offers to extract to %TEMP% and relaunch from it — the child re-runs the full gate, so loose files from the dirty folder never install; decline = exit 2; -Unattended keeps the hard abort; -LiteralPath extraction; canonical-path collision guard. Self-test 84→100 (S10 pins all five self-service paths, interactive runs driven by piped stdin with decline padding; stdin EOF = DECLINE, branch-probed on PS 5.1 — headless fails safe). Project root de-trapped; docs and contract lines synced; two-lens review, both PASS.
- 2026-07-11 r17 — comprehensive self-cleanup: installer startup rotates its own %TEMP% transcript logs (newest 10) and removes stale stamped extraction dirs; post-success launch-folder cleanup removes files byte-identical to the verified kit on offer (default No in git repositories — canonical sources; archive and non-kit files always stay; closed stdin keeps everything); install backups reported with size, opt-in prune keeping the 3 newest; strays inside kit-owned skill folders reported and kept. Self-test 100→114 (S11). Security review's execution-proven major closed: cleanup can never reach the install destination or traverse junctions, re-hashes before every delete, prunes only emptied dirs. Boundary stated: fable-system artifacts only.
- 2026-07-13 r18 — external-evidence round: the owner's fork of the fable-method repo (Sahir619/fable-method, MIT — an independent community distillation of Fable-5 practice with a 10-round/159-run eval program) was analyzed and mined; every adoption carries upstream eval evidence and passed a two-lens fresh-eyes review. Coupled pair: intent gate + artifact-authority order into CLAUDE.md Loop 5 and skill move 23 + Quick-card 13 (authority user > spec > tests > code; task framing is not intent; forced `INTENT: code does X / check expects Y / spec says Z` line in behavior-changing reports — upstream-proven 0/4→4/4 on weak executors as a forced artifact). Judge-stance verifier briefs into CLAUDE.md Capabilities and skill §6 (a completion report is claims, not evidence; the diff is ground truth; hunt weakened checks / false completion / scope creep / spec betrayal / debris). Binding minimum evidence sets into CLAUDE.md Domains and skill §1. Finance fraud table into invest-research's adversarial pass; orient-first + parallelization nuances into the skill. NEW ORGAN — `eval\` trap-suite regression harness (8 upstream fixtures adopted unchanged + attributed, adapted Workflow runner, RESULTS.md log): prime directive adopted — no rule change without the suite passing or a new failing trap first. Round-0 smoke recorded the system's first honest failure: a haiku subagent under the live system fell into the s2 spec-vs-test trap (4/8 ≈ bare-control) — dense-rulebook prose does not steer bottom-tier delegates; the proposed fix (forced INTENT artifact in the SubagentStart hook) awaits owner approval. Also: 8 canonical root files found working-tree-deleted uncommitted and restored from HEAD; refresh-kit checklist gained move-count stamps; review catches folded in (s7's second answer sheet excluded from eval staging with the lying report inlined; judge diff pinned to Git Bash; upstream round/run counts corrected). No installer, hook, or shared-threshold changes.
- 2026-07-13 r19 — open-source-prep reorganization: **the repo becomes the kit.** NEW `src\` working tree = the canonical public copy (installer, tools, docs, and `claude-home\` — the `~\.claude` payload, renamed dotless after live observation that an unpacked `.claude` tree inside the repo registers duplicate scoped skills and doubles CLAUDE.md into sessions). NEW **`FableSetup.exe`** replaces the kit zip: a small C# stub (built by `tools\build-exe.ps1` with the .NET Framework `csc.exe` every Windows box ships) embeds the whole kit, extracts to a private %TEMP% staging dir, hands off to install.ps1 with arguments passed through, propagates exit codes (0/1/2), keeps the console open on interactive runs, and cleans its staging dir; the zip model is retired (single-exe policy; the installer's legacy zip rescue remains for old archives). **Public-kit policy: no personal data ships** — project memory removed from the kit (the installer now skips it gracefully; pinned by S0/S1/S2 assertions), the MCP template ships empty (populate locally with `tools\export-mcp-template.js`), the invest-research profile line is an edit-to-taste template, and machine paths/usernames were scrubbed repo-wide (upstream MIT attribution retained — a license obligation). Installer: conditional memory step, `claude-home\` mapping, generic NEXT STEPS. Selftest 114→126: S0 slug anchor genericized + a ships-no-memory pin; S1/S2/S5 assert the empty-template contract; NEW S12 (6 assertions) builds the exe from the kit under test and installs from it unattended in a sandbox; NEW S13 (4 assertions) proves the MCP merge machinery on a fixture template with a regenerated manifest, node and PS-fallback paths both; NEW S14 (3 assertions) proves the memory machinery — install at the computed slug, gap-fill survival, -ForceMemory overwrite — on a fixture-memory kit (first-ever -ForceMemory coverage). refresh-kit skill rewritten (sync live→src, scrub gate, manifest, selftest, exe build + shipped-exe verification, single-exe policy); health-check audit 4 rewritten (live-vs-src drift + exe freshness instead of zip comparison). Docs split: kit README → `src\RESTORE.md` (restore-only), version log → this CHANGELOG, new public root README. Memory disaster-recovery is now explicitly the owner's own arrangement — the kit no longer carries it.
- 2026-07-13 r20 — **three-folder container rule + `/organize`.** The repo moved INSIDE `fable\`; beside it: `private\` (sensitive — a live-memory mirror that closes the r19 memory-backup gap with a machine-local copy, plus the real MCP template; physically outside the repo so no git operation can leak it) and `dist\` (exactly one file: `FableSetup.exe`, now untracked — `.gitignore`; attach to Releases when publishing). A container CLAUDE.md (outside the repo) carries the standing rule, including do-not-rename (the memory slug derives from the container path). NEW `/organize` skill ships in the kit and installs live — the three-folder rule as a written order: inventory → classify with reasons (in doubt → private; misfiling sensitive→public is the unrecoverable direction) → classification table before any move when a user is present → git-aware moves (tracked files via git mv; a .git dir moves with its whole tree) → subfolder tidy (folders 1–2 organized at all times) → identifier sweep of the public folder; audit mode is report-only. refresh-kit now syncs the new skill, builds the exe to `dist\`, and mirrors memory into `private\memory\` on every refresh; health-check audit 4 gains exe-location and container-layout checks; the installer's stray-scan and S1 cover the organize skill. Selftest count unchanged: TOTAL stays 126.
- 2026-07-13 r21 — folder names finalized by the owner: the working repo folder = `sharable\` (owner's pick; runner-up "main"), the artifacts folder = `EXE\`, `private\` unchanged. Every rule carrier renamed in sync: container CLAUDE.md, /organize skill, refresh-kit, health-check audit 4, README, RESTORE.md, walkthrough, .gitignore comment. The container folder itself carries the project's name — renaming it is a one-shot memory-slug migration whose procedure now lives in the container CLAUDE.md (rename → one session inside creates the new slug dir → copy old slug's memory across → verify → prune the old slug). No code-path changes; selftest stays 126.
- 2026-07-13 r22 — the three-folder rule becomes an ALWAYS-ON global rule: one bullet added to CLAUDE.md's Change Discipline (new projects and container reorganizations follow `sharable\` / `private\` / `EXE\` in a project-named container; files born in the folder matching their class, in doubt private; /organize applies and audits it). Previously the rule rode only in the /organize skill, the container CLAUDE.md, and this project's memory — none of which reach a brand-new project's session context unprompted; the global CLAUDE.md does. And an executable guarantee for the payload: `build-exe.ps1` now REFUSES a SrcRoot containing a `private\` or `memory\` directory — private content structurally cannot enter the exe (the payload embeds SrcRoot verbatim, and SrcRoot may no longer carry either). Selftest stays 126.
- 2026-07-16 — **amplification Phases 2–5** (kit contents unchanged — no rN bump; live `~\.claude` untouched). Phase 2: 18-task difficulty-gradient suite over all six dimensions + 2 domain tasks, every expected answer independently re-derived by `harness/verify_answers.py` (18 facts); new `interval_contains` calibration grader; harness selftest 62→174 assertions; live A0/A1/R run on Opus 4.8 with `claude-fable-5` as the reference bar (A0 0.965 / A1 0.972 / R 0.944 → lift +0.007, gap −0.028; run-logs in `private\amplification-runs\`). Grader-validity pass driven by live outputs: answer-first numeric extraction, markdown-emphasis normalization, widened constraint keywords — the three false failures they fixed were all on the REFERENCE condition. Phase 3: tune-by-lift-delta process exercised; first candidate (calibration addendum) rejected by its pre-stated N=3 A/B protocol — recorded null in `core/candidates/`. Phase 4: portable core extracted (`core/portable-core.md`, 11.9 KB, zero platform tokens; re-measured 0.965 ≈ full core's 0.972); grok/openai adapters implemented over verified endpoint shapes (env-var keys, never logged; live cross-model runs await keys — declared gap). Phase 5: `harness/regression_gate.py` — selftest, answer-provenance, config-drift, shared-non-negotiable lint, dated currency-baseline rungs, optional live canary — plus the standing-run instruction file `amplification/regression-gate.md`; the gate's first live run caught real `settings.json` live-vs-kit drift. Key empirical finding: bare Opus 4.8 is at ceiling on single-shot tools-off text tasks — measured lift = 0.000 (the nominal +0.007 traced by the eval-validity review to one contaminated cell: the migration task's markers appear verbatim in the injected core; task now carries a contamination warning); the one reproducible headroom dimension is calibration (interval missed the code-verified truth in 8 of 9 Opus runs; reference model passes it). Two independent fable-tier fresh-eyes reviews (correctness; eval-validity) ran on the full diff; all confirmed misgrade paths fixed (word/digit-tail keyword boundaries, markdown-tolerant answer-first numeric, infra-failure cells excluded loudly from scoring, URLError containment, gate hardening) — re-grading the stored outputs after these fixes flipped zero cells.
- 2026-07-16 — **amplification enhancement round** (research-driven; kit unchanged, live `~\.claude` untouched). A fan-out/verify research pass (`docs/research-amplification-enhancements-2026-07-16.md`; 25 sources, 22/25 claims survived 3-vote verification) mapped 2025–2026 primary literature onto the system; four greenlit items built + verified the same day. **[task] extreme-magnitude calibration class:** 6 held-out tasks (`harness/tasks-calib-heldout/`, 1e6–1e67, every truth re-derived in `verify_answers.py`; 18→24 solver facts). **[core] [0,20]-confidence-scale candidate (v3):** built (`core/candidates/v3-confidence-scale.md`) and A/B-tested live (Opus 4.8, N=3, + bare `claude-fable-5` bar) — **REJECTED by its pre-stated protocol** (v1 1.000 vs v3 1.000 interval hit-rate — a tie, no gain; the addendum measurably widened intervals 1.24×→1.53× but bare-core Opus was already at 100% coverage). Key revision: the Phase-2 "8/9 interval miss" was specific to `calib-prime-count-001`'s tight-anchor framing, **not a dimension-wide calibration gap** — the held-out A/B caught the single-task artifact before any core edit shipped. **[harness] `llm_judge` lenient-fallback grader:** now a pure verdict-parser (`graders.py`) + a blind different-model judge (`judge.py`) + `run.py --judge-fallback` (off by default, code-PASS never overridden, self-grade refused) — recovers keyword-vocabulary false negatives (research: 26%→86%→97% recall across parser types). **[harness] prompt-echo contamination guard:** `contamination_check.py` (per-grader-leg, ordered-key-in-order detection) + a `regression_gate.py` rung — auto-detects the migration answer-key-in-core artifact with no runbook false-positive; gate now 6 rungs. Verified as of 2026-07-16 (Anthropic models page): **no Opus 5 exists** — current tier Fable 5 (GA 2026-06-09, the reference bar) / Opus 4.8 / Sonnet 5 / Haiku 4.5; the currency baseline and roadmap memory were corrected. Harness selftest 174→218 assertions; regression gate 5→6 rungs, all green.
- 2026-07-16 — **amplification standing-up round** (activating the open items; kit unchanged). **API-surface sweep** → `core/adapter-targets.md` (verified 2026-07-16): xAI grok-4.5/grok-4.3 at api.x.ai/v1 (OpenAI-compat, reasoning_effort default high); OpenAI gpt-5.6/-terra/-luna at api.openai.com/v1; both keep `/chat/completions` (our portable path) while pushing a newer `/responses`; adapters take the model id explicitly (no hardcoded default). **Tight-anchor calibration class** (`harness/tasks-calib-tight/`, 4 held-out tasks — 8-digit primes, twin primes, palindromic squares, a prime band — the discriminating shape the v3 A/B showed the open-magnitude set lacked), truths in `verify_answers.py` (24→28 facts), + a `v4-tight-anchor-plan.md` candidate ready to A/B on greenlight. **Cross-model adapters** dry-run-verified end-to-end on the real suite (live blocked on user-supplied `XAI_API_KEY`/`OPENAI_API_KEY` — keys never handled in plaintext). **Regression gate registered** as the local biweekly scheduled task `fable-amplification-gate` (`13 8 1,15 * *`, diagnose-only, undo = delete the task). **Config-drift guard fixed**: settings.json is now compared on its DISCIPLINE content (the full parsed settings minus personal-overlay keys theme/tui/autoUpdatesChannel/skipWorkflowUsageWarning), order-insensitive — so it tolerates personal prefs + JSON key ordering while still catching real drift in hooks, matchers, timeouts, or any non-personal key (negative-tested for all of these). The flagged "drift" was entirely `tui` (live-only) + key ordering (the other three overlay keys already ship in the kit; hook commands + CLAUDE.md byte-identical), so the correct fix was the guard, NOT `/refresh-kit` — running it would pull the live-only `tui` pref into the public kit and gains nothing else. **Kit-membership (dossier P0.1) recommendation recorded** for the next-session Fable review: resolve via the plugin path (P0.2), not by growing the exe; interim keep expertise skills separate; decision reserved to the owner, no kit rebuilt. Harness selftest 218→235 assertions; gate 6 rungs, green against the live home. (An independent fable-tier review of this round hardened the config-drift guard to catch matcher/timeout/non-personal-key drift, not just hook commands, and tightened a task prompt + the schedule's run-cap stop condition — all fixed and re-tested.)
- 2026-07-16 — **v4 tight-anchor calibration A/B + separate-axes scoring.** LIVE A/B (Opus 4.8, N=3/arm, 36 cells; runlog in `private\amplification-runs\`): the tight-anchor class **discriminates** (plain-core baseline 0.833; `calib-primes-8digit` missed 2/3 with intervals anchored just off the truth) and **both candidate core edits eliminate the miss** — the mechanical triple-half-width rule and the [0,20]-scale addendum each score 1.000 hit-rate, Stability 1.000, zero regressions. First measured positive core delta in the program; **[0,20] addendum recommended for adoption** (owner decision; intent-gate finding on the adoption rule recorded in `core/candidates/v4-tight-anchor-plan.md` — the pre-stated majority-of-tasks letter is unsatisfiable on a mostly-saturated suite; future protocol amended to majority-of-below-ceiling-tasks). Also adopted, after an ActionAudit-style eval-design review: the **separate-axes rule** (Validity/Stability/Agreement, never one score, never optimize agreement alone) — `score.py` now computes **Stability S** (pairwise pass/fail agreement across repeated runs; selftest 235→238) — plus ecological-sabotage task design and frozen-replay principles recorded in README §3.
- 2026-07-16 **r23** — kit refresh after the core adoption below: sync check (all system files already live↔src lockstep; personal overlays excluded per policy — settings.json UI prefs, the machine-local `fable-amplification-gate` scheduled task, live profile lines), scrub gate PASS (identifiers hunted incl. username/git-identity/email; tree scanned minus .git; hits = 0 after removing regenerable `__pycache__` bytecode), plugins snapshot regenerated, manifest r23 (21 files), selftest `TOTAL: 126 passed, 0 failed`, exe rebuilt to `EXE\FableSetup.exe` and end-to-end verified (unattended sandbox install; installed CLAUDE.md hash = src hash). Walkthrough stamped r23.
- 2026-07-16 — **[0,20] calibration addendum ADOPTED into the core** (owner-approved; the first A/B-validated core change). The exact tested "Stating calibrated confidence" section appended verbatim to all three carriers — `src/claude-home/CLAUDE.md`, live `~\.claude\CLAUDE.md` (lockstep; config-drift gate green), `amplification/core/portable-core.md` (still zero platform tokens) — with the adopted kit CLAUDE.md hash-verified **content-identical (EOL-normalized) to the v4 A/B's winning-arm artifact** (which scored 1.000 hit-rate / S=1.000 vs baseline 0.833 on the tight-anchor bed). Evidence: `core/candidates/v3-confidence-scale.md` (two-stage history) + `v4-tight-anchor-plan.md`. **Kit consequence: `FableSetup.exe` is now stale vs src — the next `/refresh-kit` (rN bump) rebuilds and re-verifies it**; the fable-health-check's exe-freshness audit will flag it until then.
- 2026-07-17 — **full Fable review round** (kit contents unchanged — no rN bump; exe stays r23). Five fable-tier agents: three fresh-eyes system reviewers — correctness PASS; security/release PASS modulo the owner gates (license, history re-root — premise re-verified against pre-scrub revisions — container rename); eval-validity 5-of-6 headline claims survive full quantitative reproduction (157-cell re-grade bit-identical; 7 task truths independently re-derived) — plus the P0.1 adjudicator and a fresh-eyes reviewer of the fix diff itself. Claim revisions on the record: the [0,20] adoption reads "A/B-supported, owner-adopted" (neither pre-stated adoption rule was satisfied as written); the v2 A/B runlog was never saved (numbers permanently unauditable — provenance note added to the candidate doc); the adopted core's 18-task gradient non-regression re-run remains an open greenlight. Harness fix set (pre-re-root hash `f4265e0`, resolvable only in the local archive branch): superscript-exponent parsing + mandatory-caret ×10 form (157 stored cells re-graded, 0 flips — published numbers stand), judge-fallback≠reference guard, per-cell infra-exception exclusion, runlog core-sha256 provenance, load_tasks upfront validation, arm-aware scoring + paired lift/gap with named drops + S/V exclusion agreement, stale credential-home sweep, selftest 238→263, README §3 provenance/exclusion policy. P0.1 adjudicated + executed (pre-re-root hash `697e1b0`, resolvable only in the local archive branch): plugin path adopted, staged — a `fable-expertise` skills-dir plugin validated fail(11/25)→pass; the 11 strict-YAML-invalid expertise descriptions quoted in repo and live `~/.claude/skills` in lockstep (`claude plugin validate` EXIT=0; `lint-structure.ps1` ALL PASS); namespacing falsifier resolved negative at the mechanical level (the classifier matches prompt keywords, never skill names); full P0.2 exe-retirement deferred — plugins cannot carry a root CLAUDE.md, settings hooks, or scheduled tasks (verified at the plugins docs, 2026-07-16). Known follow-ups: kit-src `organize` description carries the same latent YAML pattern (route via a kit-pipeline round); live namespacing A/B and the gradient re-run are budget greenlights.
- 2026-07-17 — **fable-expertise plugin manifest + live-run attempts** (kit unchanged; exe stays r23). Executed the two greenlit follow-ups from the review round. (1) **Plugin install posture RESOLVED + shipped (zero-budget):** `expertise-system/.claude-plugin/plugin.json` (`fable-expertise` v0.1.0) makes the expertise layer a validated, installable Claude Code plugin — `claude plugin validate` EXIT=0 (only the intentional missing-author warning; author omitted to keep the tree PII-clean), expertise `lint-structure.ps1` ALL PASS, regression gate 6/6. Posture = **coexist** with the bare-named live skills (retaining originals means namespacing cannot break description-keyed auto-triggering). (2) **Gradient non-regression re-run + live namespacing A/B — ATTEMPTED, INFRA-BLOCKED:** both need live `claude -p`; the CLI's OAuth session expired mid-program and its refresh token is rejected server-side (`claude auth status` → `loggedIn:false`), so all 36 gradient cells failed authentication in their isolated homes and were correctly excluded (no false zeros — a second live proof of the infra-exclusion guard). Void runlogs discarded; no numbers changed. **Both live checks are pending `claude auth login`** — a one-command repeat each. The adopted core's broad-suite "zero regressions" therefore remains bed-proven (4 tasks) with the 18-task gradient confirmation pending; the namespacing pass is mechanically argued (~85%) with live confirmation pending.
- 2026-07-17 — **both live A/Bs completed (auth restored).** After `claude auth login` restored the CLI session (liveness re-proven with a real `claude -p`, not just `auth status`, and the on-disk access token confirmed ~8 h fresh), the two blocked runs executed on Opus 4.8. (1) **Gradient non-regression A/B — 0 regressions:** base core vs adopted core, both fresh, 18 tasks × A1 = 36 cells, N=1/arm (the two cores differ by exactly the 13-line addendum). 0 infra-exclusions, 0 regressions; 16/18 cells identical (14 at ceiling), 2 N=1 rises NOT claimed as addendum benefits (one the known contamination cell, one a base-arm injection-miscount the calibration addendum has no mechanism to touch). The broad-suite "zero regressions" the v3 protocol required is now confirmed at N=1, no longer bed-proven only. Runlogs in `private\amplification-runs\phase6-*`. (2) **Namespaced auto-trigger A/B — PASS:** a bare finance prompt auto-invoked `finance-value`; the same prompt with the plugin loaded auto-invoked `fable-expertise:finance-value`; a router prompt auto-invoked `fable-expertise:expertise-atlas` — identical triggering bare vs namespaced, so the coexist posture is confirmed live (mechanical falsifier resolved negative). Kit unchanged; exe stays r23.
- 2026-07-17 **r24** — CLAUDE.md wording reconciled with reality (owner-requested via /refresh-kit). Two changes to the universal `~\.claude\CLAUDE.md` (synced to `src\claude-home\CLAUDE.md` in lockstep): (1) the three-folder rule no longer calls `private\` "physically outside any repo" — `private\` now keeps its OWN local-only, never-pushed git repo for version history (created this session: no remote + `push.default=nothing` + a `.git/hooks/pre-push` block that refuses any push, transfer-tested at 0 refs), so leak-safety is the enforced no-push, not the absence of git; (2) the two-level CLAUDE.md model is now explicit in the Priority section — the universal `~\.claude\CLAUDE.md` (home-relative, user/machine-independent, governs every project) versus an optional per-project `CLAUDE.md` at the project root (rare) that extends/overrides it for one project. The container `CLAUDE.md` gained a matching header note and private-folder wording. Refresh: sync live→src (CLAUDE.md only; settings.json personal overlays correctly not synced), scrub gate PASS (username/git-identity/email hunted, 0 hits), plugins snapshot regenerated, manifest r24 (21 files), selftest `TOTAL: 126 passed, 0 failed`, exe rebuilt to `EXE\FableSetup.exe` (103,424 bytes) and end-to-end verified (unattended sandbox; installed CLAUDE.md hash = src hash). Wording only — no installer, hook, threshold, or assertion-count change. Process note: the refresh-kit skill lists src-resident doc stamps at step 8 (after the manifest/build) which contradicts its own "nothing in src may change after the manifest" invariant — doc stamps belong before the manifest; flagged for a skill fix.
- 2026-07-17 **r25** — **installer upgrade hygiene: previous-version cleanup via an install ledger** (owner-directed: the exe must "clean previous old versions" on fresh or in-use machines — before this round, files an older kit installed but the new kit no longer ships survived every upgrade, pinned by S11d's keep-behavior). install.ps1 gains step 4/9: every install writes `.claude\fable-install-ledger.json` (the claude-home payload it placed, path + sha256; settings.json and .credentials.json never managed); the next install removes files a PREVIOUS kit installed that the new kit no longer ships — only while still byte-identical to that recorded hash (backed up to the run's backup folder first, emptied dirs pruned, never through reparse points, never settings/credentials/memory/backups; corrupt ledger → cleanup skipped with WARN, fresh baseline ledger heals it) — while user-modified and user-created files are always kept (INTENT: code kept all strays / user directive: clean previous old versions / resolution: remove only ledger-proven unmodified kit files). Post-install stray-scan now derives kit-owned folders (skills\* + scheduled-tasks\*) from the manifest instead of the hardcoded four-skill list. Two independent Fable-tier reviews ran on the diff (correctness PASS; security FAIL on one confirmed finding, fixed and re-verified before shipping): the cleanup guards now canonicalize each ledger path via `Get-Item`'s real on-disk `FullName` and refuse any non-canonical entry BEFORE the protected-name/managed checks — `[IO.Path]::GetFullPath` strips trailing dots/spaces, so a crafted ledger entry like `settings.json.` or `projects.\memory\x` had resolved past the string guards to a real file; now blocked (pinned by S15d, which also proves a crafted managed/`settings.json` entry is refused, never deleted). Also from the reviews: `Backup-IfExists` switched to `-LiteralPath` so a wildcard-metacharacter path (`[ ]`) can no longer silently no-op the backup then delete; an empty or files-less ledger now reports WARN + skips cleanup instead of a silent baseline PASS; the startup banner notes backups cover cleanup too. Selftest 126→141: S1 pins ledger presence/version/unmanaged-settings + kit-hash provenance, S12 pins the exe path writing it, NEW S15 (12 assertions) proves the full arc on a fixture rOLD kit — stale file removed on upgrade + backed up + named in output + dropped from the new ledger; user-edited stale file kept with note; corrupt ledger heals safely; adversarial trailing-dot names refused; S11d's user-stray keep behavior unchanged. refresh-kit skill: src-resident doc stamps moved BEFORE the manifest (closing r24's recorded contradiction; the count-change contract now also covers the `docs\` pages and a repo-wide old-count grep) and shipped-exe verification now also checks the sandbox ledger. organize skill kit copy synced to the live genericized wording. Scrub gate PASS (username + git identity, 0 hits). Scope limit (declared): the FIRST r25 install finds no ledger and writes a baseline only, so pre-r25 debris is not retroactively cleaned — cleanup is prospective (r25→r26 onward). Baseline first re-verified this session: unchanged r24 selftest `TOTAL: 126 passed, 0 failed`.
- 2026-07-17 — **amplification harness hardening** (kit unchanged — no rN bump; `amplification/` is not in the kit manifest, so `FableSetup.exe` and the discipline files are untouched). A four-reviewer Fable-tier sweep (grader correctness, harness infra + secret-safety, whole-installer + hooks, cross-system consistency) surfaced grader bugs whose fixes are the science's keystone: **(1)** `graders.py` `_contains` decimal-boundary — the bare keyword `0` matched the fractional `0` inside `100.0`, so an incomplete "water boils at 100.0" (freezing point omitted) PASSED; the leading digit-boundary now also excludes `.`, so `0` no longer leaks while `100` still matches `100.0`. **(2)** `interval_contains` (the one measured-positive/calibration axis) now tolerates a leading approximation marker (`P5: ~450,000`, `about`, `approximately`) — a substance-preserving estimate had scored 0.0 as "no interval" — and rejects a non-finite overflow bound (`1e400`→inf) that would otherwise make any truth "contained". **(3)** `verify_answers.py` gains the arithmetic-series re-derivation (203, by day-by-day simulation) so the provenance gate's "every code-checkable value is re-derived" claim is finally true (28→29 facts). Infra: **(4)** `run.py` per-cell adapter exception boundary widened so a malformed HTTP-200 body (JSONDecodeError / truncated read) excludes just that cell instead of aborting a whole budget-spending run, and an all-cells-excluded run now exits non-zero (a green process signal on a no-usable-data run was dishonest); `adapters.py` treats an `{"error":…}` 200 envelope and a `content:null` turn as infra exclusions, not false-zero fails; **(5)** the regression-gate contamination and currency rungs now FAIL when they would guard nothing (zero cores / zero dated facts) instead of vacuous green. Harness selftest **263→268** (5 grader pins), verify_answers **29 facts / 0 failures**, and — the mandatory science-integrity check after touching a grader that produced a published number — a re-grade of all 8 stored runlogs (**193 scored cells, 81 interval cells**) flipped **zero** grades: the **+16.7 pp** calibration result and every other figure are bit-identical under the fixed graders (the bugs were latent on the actual stored outputs). Deferred with notes: cross-vendor judge routing no-op + alias self-grade guard (off-by-default judge path); `numeric which:"last"` picking a trailing timestamp on annotated injection answers (heuristic fix, needs care); the redactor-flag and classifier-stem items ship in the kit round below.
- 2026-07-17 **r26** — **second error-sweep-and-upgrades round: kit fixes from the same four-reviewer sweep** (the harness half shipped in the entry above; this is the kit-content half, so it bumps rN and rebuilds `FableSetup.exe`). **Security (redactor, `export-mcp-template.js`):** the MCP-template redactor missed credential secrets in space-separated form — `-H "Authorization: Bearer …"`, `-u user:pass`, and compound flags `--auth-token`/`--api-token` all leaked into the "clean" template (the publish surface), because `CRED_FLAG`'s full-match alternatives didn't cover compound/short flags while `CRED_NAME` (used for `=` forms) did. A `credFlag()` helper now applies the substring logic to the space-separated form too, adds `-H`/`-u`, and a new `CONN_SECRET` pattern catches `;`-delimited connection-string keys (`AccountKey=…`); `--auth-mode`/`--user` and inline scripts stay preserved (execution-proven; +6 S7 fixtures). **Enforcement (classifier v7→v8, `settings.json`):** the prompt classifier silently missed obvious High-risk prompts — `disable tls verification`, `git reset --hard`, `restore prod from backup`, `fix the sql injection`, `chmod 777`; added stems `\btls\b|\bssl\b|certificat|inject|revert|reset.?--?hard|\brestore\b|\bbackup\b|chmod|chown|permission` (src + live byte-identical; pipe-tested to fire, existing stems + silence preserved; +4 S9 tests). **Installer robustness (`install.ps1`):** the install-ledger write is now per-entry resilient — one locked/unreadable managed file carries its prior provenance forward instead of aborting the whole ledger; and a failed `settings.json` merge now AUTO-ROLLS-BACK to the pre-install backup (the header promised "automatic rollback"; validation failure previously only advised). **Consistency (`fable-health-check`, `organize`):** the monthly audit now inventories + hash-checks `organize` (the one kit skill whose drift it couldn't see); `organize`'s private-folder model reconciled with CLAUDE.md's r24 own-local-repo wording; two skill descriptions had their strict-YAML-invalid `": "` replaced with `" — "`; RESTORE.md gained the ledger-cleanup behavior with its prospective caveat; the `docs\` pages carry a kit-version stamp (making refresh-kit step 9 truthful). Selftest **141→156** (+6 S7 redactor + 4 S9 classifier; a fresh-eyes review of the fix diff then added 5 more S7 pins after catching a `python -u` over-redaction and a `--user user:pass` leak in the first redactor cut), exe rebuilt to `EXE\` and re-verified end-to-end incl. the live upgrade path. Scrub gate PASS. Deferred with notes (fail-safe / low-frequency / cosmetic): no-node in-use MCP merge (node is a stated prereq), read-back validation for a malformed `.claude.json`, build-exe hidden-file name-set check, ledger-cleanup sub-ms TOCTOU, selftest crash-vs-FAIL on an absent ledger.
- 2026-07-17 **r27** — **QC + global-consistency round (owner-requested full audit → fix-all → deeper-look enhancements).** A four-auditor sweep (live↔src sync, kit-internal, doc cross-consistency, memory/plugin/private) then three fresh-eyes reviewers + one adversarial re-check drove this round; every claim below was execution-verified. **The headline fix — reversion hazard:** r26's `organize` + `fable-health-check` improvements existed only in `src\` and had never been deployed to live, and refresh-kit step 1 was mechanical "live-wins", so the next `/refresh-kit` would have silently reverted them. Step 1 now decides sync **direction by content, not mtime** (git doesn't preserve mtimes): `git status` catches uncommitted src edits, an install ledger settles committed cases, ambiguity STOPS and asks, and settings.json is never blind-copied (hand-port live hook improvements into src, then deploy via the installer merge which would otherwise replace a newer live hook). Live was reconciled this round: all six discipline files deployed src→live (backed up first), personal overlays (invest-research themes, settings.json `tui`) preserved. **Consistency fixes:** health-check now compares settings.json on discipline content minus the four personal-overlay keys (killing a permanent false-DRIFT loop on the live `tui` pref, triaged per-key), audits `organize`, checks `private\.git` for *efficacy* (remote + hook-refuses-grep + `core.hooksPath` unset) not mere presence, exempts the harness `.claude\` dir's existence while flagging any hooks planted inside it, and audit 1 now covers the intent-gate authority order + the 0–20 confidence section; refresh-kit step 9 sweeps every changed count (installer + harness + facts) and step 10 mirrors memory **additively** (a live-pruned memory must survive in the backup); the two r26-stamped docs pages had stale harness counts (263→268 assertions, 28→29 facts) corrected; README now inventories `amplification\` + `expertise-system\`; the eval prime directive gained an explicit scope (semantic changes gate; wording-only + classifier-stem edits exempt but CHANGELOG-logged); the expertise system's kit-membership "open decision" marked RESOLVED (plugin/COEXIST). CLAUDE.md rule fixes: an airtight skill-authority rung (a skill adds method, never relaxes any rule here, never overrides Rank 0; stricter reading governs), a file-level + durable-backup memory-deletion "recoverable" definition (a dir mirror that predates the file, or a rolling mirror the next sync could delete, is not coverage), and the confidence-vs-probability boundary disambiguated on the (non-measured) Calibrate bullet — the A/B-measured 0–20 section itself left byte-identical. **Enhancements adopted** (reverse-propagated from the portable core / eval-proven): a planted-premise rule (Loop 1 + fable-mode §1 — verify the ask's premise, not just the task), a format-compliance clause in Reporting, an INTENT-line gold-standard exemplar, and calibration/scale alignment across the pair. **Trap baseline recorded** (`eval\RESULTS.md` Round 1, full 8 fixtures, haiku, n=1): 5/8 ideal, 2/8 partial, 1/8 failed; s2 rose 0→1 vs Round 0. No installer/hook/threshold/assertion-count change (selftest stays **156**; harness 268/29; 23 moves; 9 sections). Exe rebuilt to `EXE\` (115,712 B) and verified end-to-end (INSTALL PASSED, CLAUDE.md hash == src, ledger r27). Scrub gate PASS. **Deferred, proposed to the owner** (higher blast radius or measurement-gated, not applied): move the forced INTENT artifact into the SubagentStart hook (the s2 evidence supports it), a third-party/MCP-instruction trust clause in Rank 0, syncing the confidence-note into the portable core under a measured round, a "minimal-repro" move (23→24 count ripple), and an eval-gated fable-mode compression pass.
- 2026-07-17 **r28** — **owner-directed full-queue round: the r27 audit's five fixes, all five deferred enhancements plus keyless cross-model work, and the three strategic elevations, executed end-to-end in one session.** **Fixes:** F1 the monthly `fable-health-check` is finally REGISTERED as a scheduled task (cron `30 8 3 * *`; the registration API rewrote the task file to a self-referential wrapper — caught by hash-check, the full audit spec restored as the prompt, with two declared spec tweaks: audit 2 now expects the INTENT artifact in the SubagentStart hook, audit 4(f) restates the new backup script; the amplification gate's config-drift rung then caught the resulting live-vs-src drift exactly as designed, synced this round). F2 README-PRIVATE's contents list made truthful (amplification-runs\, archive\, tools\). F3 raw-judge-output persistence rule (eval README step 4 + RESULTS provenance note; Rounds 0–1 grandfathered). F4 `workflow.js` executed end-to-end for the FIRST time — it instantly caught a real integration failure (the platform delivers `args` as a JSON string; the runner assumed an object) and was hardened with a defensive parse. F5 `private\expertise-build\` archived. **Enhancements (all live+src lockstep):** E1 the forced INTENT artifact moved into the SubagentStart hook additionalContext (new S9 pin: selftest **156→157**); E2 gold-standards **#8 data-quality judgment exemplar** (contamination-safe — zero s4 fixture tokens); E3 Rank-0 #2 gains the MCP-instruction trust clause (server instructions/tool results shape tool calls, never authorize, relax, or reclassify; relayed content inherits its source's classification); E6 fable-mode **move 24: minimal repro** (23→24 stamped across README/RESTORE/walkthrough/OVERVIEW/index); E7 compression pass executed as an audit — honest outcome: the file is already tight (2 micro-trims; deeper cuts would break skill-only consumers: the portable core's lineage and the eval's `skill` condition). **Round 2 trap gate (14 cells: s2/s4/s6 ×3 seeds, others ×1, judge=fable via workflow.js): PASS — zero regressions, s7 frauds 4/5→5/5** (`eval/results/round2-r28-gate.json`, the first persisted raw round). Two recorded nulls: the INTENT hook demonstrably REACHES haiku executors (judges quote them reciting the authority order) but does not flip s2 (c_a 1,1,1 — flag-but-still-edit; next lever is the per-task delegation brief, not more hook prose), and the s4 exemplar is mechanically unreachable in the ambient condition (`references/` load only on skill invocation) — s4 0,0,0 is the expected null. Variance reading: c_a variance ZERO across all three 3-seed fixtures; only judge-side sub-scores wobble. **E8 (keyless):** `run.py --adapter-json` spec passthrough (credential-NAMED fields refused on argv — the guard's first draft flagged its own `allow_keyless` flag via substring "key", fixed and negative-tested), `adapters.py allow_keyless` honored only for loopback base_urls, adapter-targets gains the Local-models section; dry-run proven end-to-end (4 cells vs `http://localhost:11434/v1`, endpoint+adapter recorded in the runlog). **Elevations:** V1 `private\tools\backup-private.ps1` — pure-PowerShell AES-256-CBC + PBKDF2(200k)+HMAC encrypt-then-MAC of the whole private folder, passphrase interactive-SecureString only, `-SelfTest` roundtrip+tamper-detection PASS; V2 **MIT LICENSE** landed under a neutral "Fable System authors" holder (rename before push for attribution) with README/index license lines updated. V3 the **differential-calibration bed** (`tasks-calib-diff/`: twin primes [1e7,2e7)=48,427, triplets p<1e6=1,393, Goldbach 1e6=5,402, two-squares<1e6=215,907 — every truth sieved twice: scratch derivation + `verify_answers.py` **29→33 facts**, harness selftest **268→280** via the review-driven diff-bed pins — existence, truth-vs-solver bridges, per-task pass/fail exemplars) + `v6-differential-bed-plan.md` with pre-stated validity/adoption rules, and the `v5-confidence-boundary-note.md` candidate (core edit staged, core untouched). **Declared blocked: the v5 and v6 measured A/Bs — `claude auth status` → `loggedIn:false`; one `claude auth login` unblocks both.** Amplification gate: the first run correctly FAILED on the deliberate health-check drift (caught-then-synced), then green 6/6; the contamination guard was EXTENDED to scan every `tasks-*` dir (**34 tasks, was 20** — closing a pre-existing blind spot over the calibration beds, including the +16.7pp tight-anchor suite): 1 known/0 new against both cores. **Two independent fable-tier fresh-eyes reviews** ran on the full diff (correctness; security+edges): security PASS with 2 major hardening defects (URL userinfo defeated the loopback-guard string-parse; a userinfo credential could ride `base_url` into the persisted runlog endpoint) + 3 minors; correctness FAIL with 3 majors (the new bed shipped selftest-unpinned; the original contamination claim was vacuous; `--adapter-json` could silently override the R reference bar and `dry_run`) + 4 minors — **all findings fixed and re-proven by execution**: urlsplit-hostname guard + dual-layer userinfo refusal (adapter raise + argparse reject), `[::1]` restored, PBKDF2 200k→600k, parameter-path passphrase minimum, the +12 selftest pins, the extended gate scan, R-cells excluded from the spec merge + `model`/`dry_run` keys refused, CHANGELOG LF restored, walkthrough history-row order fixed, RESULTS s3 wording corrected. Installer selftest `TOTAL: 157 passed, 0 failed`; exe rebuilt to `EXE\` and end-to-end verified; scrub gate PASS.
- 2026-07-17 — **v5/v6 measured A/Bs completed (auth restored; kit contents unchanged — no rN bump).** **v5 ADOPTED:** the confidence-vs-probability boundary note appended to the portable core's Calibration bullet after passing both pre-stated legs — tight-anchor bed **12/12 contained** (hit-rate 1.000 = the stored winning arm, S=1.000, N=3) and the 18-task gradient spot **17/17 identical on scorable cells, 0 regressions** (the only movement, 1→0.875, sits inside the pre-excluded contamination cell `plan-zero-downtime-migration-001` — the exact `KNOWN_CONTAMINATION` pair); the shipped `portable-core.md` is hash-identical to the tested winning arm, regression gate 6/6 green against it. **v6 NULL — the falsifier resolves NEGATIVE:** Arm A (adopted core, N=3) contained all four differential truths **12/12**, so the bed does not discriminate, Arm B never ran (validity gate closed — no budget on an invalid bed), and the mechanical widen rule is retired for lack of constituency. The calibration-headroom program is complete: v3 saturated, v4 the one real gap (closed by the adopted [0,20] section), v6 no residue. 42 live Opus cells total, 0 infra exclusions; runlogs `private\amplification-runs\phase7-*.json`.
- 2026-07-17 **r29** — **post-audit hardening round** (owner-directed: a five-axis container audit — kit-drift, docs-accuracy, publish-surface, private-side, live-harness — then "execute and commit all of them"). **Publish-surface hardening:** the scrub gate (refresh-kit step 2) now purges regenerable `__pycache__` bytecode FIRST (it embeds absolute machine paths and regrows on every harness run; the audit found 8 `.pyc` carrying the machine path — git-ignored so push-safe, but a raw-folder/zip leak, and regrown since r23's manual cleanup), sweeps the ENTIRE repo tree (was src+eval+root docs; `amplification\` and `expertise-system\` — ~140 published files — were previously out of the gate's named scope) on identifiers AND class patterns (emails, user paths, credential shapes; exactly two exemption classes: upstream attribution + the installer's synthetic S7 fixtures), and checks the GIT LAYER — commit author/committer identity and date offsets, which no file sweep can see. **`master` re-rooted once more to UTC dates:** all three public commits carried `-04:00` offsets (a coarse location signal, immutable after first push); rebuilt via `commit-tree` with trees and messages verified byte-identical — only the offsets changed (first attempt left a trailing-newline diff in messages, caught by the byte-check and redone from `git cat-file` raw bytes); local recovery ref `archive/pre-utc-master` kept, never push. **Pre-push guard:** `sharable\.git\hooks\pre-push` now refuses any ref except `refs/heads/master` (fixture-tested: archive ref exit 1, master exit 0, mixed `--all`-style batch exit 1) — the pre-scrub `archive/pre-publish-history` branch was previously protected by procedure alone. **Build receipts:** step 8 additionally verifies the exe's VERSIONINFO is identifier-free and appends a receipt to repo-root `build-receipts.jsonl` (kit rN, exe sha256+bytes, selftest total, sandbox verdict) — before r29, no evidence of the end-to-end sandbox verify survived the sandbox's deletion; the r29 receipt is the first. **Audit accuracy fixes:** stale pre-re-root wording corrected in the container CLAUDE.md and `expertise-system/MANIFEST.md` (the r28 re-root docs commit missed both), core size ~43→~46 KB in the OVERVIEW/index.html feature tables (actual 47,091 B; the Efficiency sections still said ≈43 KB and were corrected in r30), portable-core "11.9 KB" → "11.9 at extraction / 12.8 KB today" (the adopted v5 note grew it), regression-gate run cap restated for the real cadence (1st+15th = 24 runs/yr; 26-run cap ≈ 13 months), the advancement dossier's P1.2 gains its missing STATUS line (the INTENT hook shipped in r28; its other three items remain open), `backup-private.ps1`'s header now says the 600k PBKDF2 iterations its code always implemented, and README-PRIVATE documents the six early `&&` author-name commits (email neutral throughout; repo local-only). Audit clean-bill on record: kit↔live drift zero beyond the two documented personal overlays; manifest 21/21; expertise repo↔live 51 files hash-identical; memory 10/10 identical live↔snapshot with 9/9 consistent scope carriers; publish sweep zero identifier/secret hits across tree, all master commits, and the exe's decompressed payload. No installer/hook/threshold/count change: selftest `TOTAL: 157 passed, 0 failed` (run twice — before and after the review fixes); manifest r29 (21 files); exe rebuilt to `EXE\` (119,296 B) and end-to-end verified (INSTALL PASSED, installed CLAUDE.md hash == src, sandbox ledger r29, VERSIONINFO clean). Hardened scrub gate first run: PASS (208 files swept; 8 hits, all known synthetic fixtures; git layer neutral identity, all UTC). **Two-lens fresh-eyes review:** security/release lens PASS with findings fixed and re-proven in-round — the pre-push hook's `archive:master` refspec bypass and master-delete hole closed (source must now be the local master tip; probe battery 7/7), the gate's git-layer format gained committer fields (`%cn|%ce` — committer contamination is the classic rewrite miss), `Z`-offset wording corrected, a fixture-fakeness marker rule added (unmarked high-entropy values in the exemption files are hits), binaries declared in-scope for the class sweep, and a README note that hooks never travel with clones. The correctness-lens reviewer was infra-terminated mid-run by an API session limit; its hunt-list was executed as a declared self-review fallback (checkable claims re-derived by execution; stale-reference sweep; scope-map of every hunk to its audit finding) — an independent re-run remains available after the limit resets.
- 2026-07-18 **r30** — **whole-repo audit round** (owner-directed: "evaluate all folders and files ... cover all blind spots, ensure global consistency, fix and commit"). Twelve independent fable-tier reviewers, one per area (root docs, kit core, kit skills, installer, build tools, amplification program, amplification task truths, eval suite, expertise infrastructure, expertise content ×3, global cross-file consistency), every finding adjudicated against the primary source before it was applied or killed. **Two publish-surface leaks closed, both reproduced by execution first.** (1) `build-exe.ps1`'s integrity gate was **one-directional**: it verified that every manifest-listed file was present and fresh, but never that the tree contains *only* manifest files — and `CreateFromDirectory` zips the directory verbatim, hidden files included. An unlisted stray (a personal note, a `desktop.ini`) would have been embedded in a published exe, and every install from that exe would then have failed the installer's own set-equality gate. It now sweeps the tree with `-Force` and refuses to build; proven on both a visible and a hidden planted stray. The security review then defeated that first version with a **directory junction** — `Get-ChildItem -Recurse` does not traverse a reparse point but `ZipFile.CreateFromDirectory` follows it, so a junction smuggled its target into the payload past both the new sweep and the older `private\`/`memory\` check (enumerator saw 0 files; the zip carried the linked file). The gate now refuses any reparse point under `SrcRoot`, re-proven against the same exploit with a clean kit still building. (2) The MCP-template redactor missed the two Google credential classes the scrub gate explicitly names: `ya29.` tokens (the dot defeats the whole-arg matcher) and `AIza` keys inside URLs or compound args — 3 of 4 shapes leaked in a marked-fixture probe. Fixed in `KEY_PREFIX`, re-proven 4/4 with the benign-input output byte-identical to the pre-change baseline. **Expertise system:** nine skill descriptions exceeded the platform's documented **1024-character cap** (max was 1237) — a hard validation failure on API/claude.ai upload that Claude Code silently tolerates, so nothing in the repo noticed. All 25 now fit (max 1024); the nine build briefs were re-synced to match ("copy EXACTLY" is their contract), the authoring template now states the cap, and `lint-structure.ps1` gained the check that would have caught it, plus duplicate-heading and unknown-skill-directory checks (all three proven to fire on a broken fixture). Also: `audit-coverage.ps1` no longer crashes on a brief missing its name line; the unreproducible "410 refs / 30 distinct" metric is replaced with the claim its check can actually support; stale `TEMPLATE.md` pointers fixed in all 24 briefs. **Installer:** a no-op MCP merge no longer rewrites the user's whole `.claude.json` (the public kit ships an empty template, so that path is normally a no-op); `-LiteralPath` applied to the copy, verify, and memory paths (a bracketed home directory broke hash-skip idempotence and produced spurious verify FAILs); the integrity gate and copy loop enumerate hidden files; the extraction pre-clean gained the reparse-point guard every other delete path already had; a literal `null` hook group is dropped rather than re-serialized — in BOTH merge implementations, since the first pass fixed only the PowerShell fallback and left `fable-merge.js` re-serializing the null, which would have left the two paths disagreeing on the same input. The `-LiteralPath` pass also now covers the settings.json copy, merge-read, and validation-read, so a bracketed home directory installs cleanly (`exit=0 / INSTALL PASSED`) where the r29 kit aborted with `settings.json validation FAILED` — that failure was pre-existing, not introduced here. **Amplification:** the keyword grader's trailing digit-guard was value-blind — bare `"0"` matched `"0.5"` and `"rows=5"` matched `"rows=5.2"` — now guarded value-aware (a trailing `.0` still matches); `__bold__` phrasing no longer false-fails (the underscore sibling of the live Phase-2 bold bug), with `contamination_check` normalization kept aligned — and the same fix closes an evasion in the other direction, since a forbidden word wrapped in `__…__` previously slipped past a `contains_none` leg unseen. A differential re-grade of all 34 task files through the committed and new graders produced 42 changes, every one traced to these two fixes; value-preserving matches (`100`→`100.0`, `rows=5`→`rows=5.0`) and single-underscore identifiers are unaffected. That claim is reproducible rather than asserted: the round ships the harness that makes it — `harness/grader_diff.py`, zero-budget, which loads any reference graders.py beside the working one and prints every disagreement (`python amplification/harness/grader_diff.py` → `tasks: 34 | grade calls compared: 657` / `DISAGREEMENTS: 42`). **Stored-runlog caveat:** the run-logs behind the published §7 numbers live in `private\` and were NOT re-scored this round, so a future re-score of stored cells could move individual cells in both directions (the `__bold__` fix can turn a stored FAIL into a PASS, the value guard a stored PASS into a FAIL); `calibration_underdetermined`'s `contains_none` leg dropped (it punished the disciplined answer that states the attainable range — the same pattern already removed from two sibling tasks); adapters now actually record `tools` in meta as three docs claimed; `schema.md` documents `judge_reference` and the interval grader's first-match rule; stale "Opus 5" host references replaced (the repo's own currency baseline records no such model). **Expertise content** (beyond the description cap): finance-value gains IFRS 18 (issued 2024, effective 2027 — new P&L subtotals and management-defined performance measures, retrieve-current-dated); medicine-clinical-health pre-specifies the ICH E9(R1) estimand (population/treatment/variable/summary/intercurrent-event strategy) ahead of endpoint and analysis population, and states Little's Law with flow time rather than the station-level "cycle time" its sibling ops skill explicitly cautions against; dentistry's split-mouth variance passage split into its conservative and anti-conservative cases (the algebra was already correct); industry-sector-mastery registers "health technology" in coverage and adds industrial tech to the description, matching its body. **LICENSE**: the third-party fixtures notice moved out of the middle of the MIT body to a trailing section, so the MIT text is verbatim and contiguous (mid-body insertions commonly defeat automatic license detection). **Docs:** `index.html`'s publishing section still told the reader to re-root history and choose a license — both long since done, contradicting the README and its own credits line; size claims, cadence wording ("biweekly" → the actual 1st + 15th), keyless-adapter omissions, dangling pre-re-root commit hashes, and an orphan citation all corrected. Verification: installer selftest **157 passed, 0 failed**; harness selftest 280/280; `verify_answers` 33/33; contamination 1 known / 0 new (the bare checker exits 1 on the documented migration-task hit; the gate's `--allow` list is what yields "0 new"); expertise lint 25/25 and coverage 24/24 (0 missing terms). No hook, threshold, or assertion-count change. **Refresh completed the same day** (`/refresh-kit`, after the commit above): the six discipline files deployed `src\`→live through the installer (9 files backed up to `fable-install-backup-20260718-060057-405`; personal overlays `tui`/`theme` and the live invest-research profile line preserved — the installer reverts that line by design, so it was captured before and restored after), the 12 corrected expertise skills deployed to live (51-file backup taken first), and **`FableSetup.exe` rebuilt to r30 and verified end-to-end** in a disposable sandbox: exit 0, `INSTALL PASSED`, installed CLAUDE.md hash equal to `src\`'s, sandbox ledger reading r30, VersionInfo identifier-free — receipt appended to `build-receipts.jsonl` (122,368 B, sha256 `2B9BAB86…AFC0`, matching the artifact on disk). **Publish route:** by `git push` of `master` only — the tracked tree and every commit are identity-neutral, but `.git\logs\` still carries the pre-scrub author identity and local-TZ offsets, so a raw folder or zip copy is NOT publishable; the scrub gate in refresh-kit now names that second carrier explicitly. **`FableSetup.exe` rebuilt to r30 and end-to-end verified** (see the refresh note above).
- 2026-07-18 **r31** — **deferred micro-queue executed in full** (owner: "did you commit all fixes and on hold tasks? covering all blind spots?" — the r30 QC pass had deferred these by name; deferral overridden). **Redactor:** four residual credential-prefix classes join `KEY_PREFIX` (GitLab `glpat-`, Hugging Face `hf_`, DigitalOcean `dop_v1_`, npm `npm_`); mysql-style attached `-p<password>` redacts while `-p<digits>` port forms survive; and the **unknown-field walker** closes the structural gap the security review proved — fields outside the known server schema (a hand-added `apiKey`, a nested `auth.token`) previously rode into the template verbatim through the deep copy, now every string leaf outside the schema is credential-checked (key name and value shape both). `BARE_TOKEN` deliberately stays at 32 chars — documented in-code: shorter whole-arg opaque strings are too often benign ids, and real sub-32 tokens ride behind prefixes or flags, which are caught. S7's five unmarked fixture values renamed to carry self-describing LEAK markers, satisfying the scrub gate's own checkable-fakeness rule. **Classifier v9:** clinical stems (dose/dosage/clinical/patient/diagnos/contraindic/prescri/symptom/medicat/dentist/dental) close a verified blind spot — a clinical-numbers prompt stayed silent under v8 despite Medicine/Dentistry being a never-from-memory rigor domain; plus `stdin.setEncoding("utf8")` against multi-byte chunk-boundary corruption. Pipe battery: 4 clinical prompts fire, 3 controls silent, v8 stems unregressed. **Installer:** PowerShell-fallback settings merges are change-detected — a no-op re-run leaves settings.json byte-untouched, matching fable-merge.js's contract; new **S5b** scenario pins the real-shape `.claude.json` (empty-string key) on the no-node path, where Windows PowerShell 5.1's ConvertFrom-Json throws — the installer must degrade loudly and leave the file byte-preserved (it does; execution-pinned). **make-manifest:** ordinal sort (a culture-sensitive sort could produce spurious manifest diffs across locales) and manifest self-exclusion by full path (a nested file named kit-manifest.json is kit content). **Stub:** `-Unattended` accepts unambiguous prefixes like PowerShell's binder (no more "Press Enter" block on `-u` runs); tab joins space/quote in the quoting fast-path. **Selftest 157→171** (10 S7 redactor pins + 3 S5b + 1 S9 v9 pin); docs re-stamped (README, RESTORE, walkthrough, OVERVIEW, index.html, refresh-kit step 6). Exe rebuilt to r31 and end-to-end verified; receipt appended.
- 2026-07-18 **r32** — **adversarial-reach pins made durable** (owner: "anything else you want to check or enhance?" — completeness pass). The r31-review's strongest verification evidence — the ledger-cleanup **traversal** (`..\`) and **through-junction** refusals, execution-proven against the shipped exe with TRUE-hash worst-case entries — existed only in session scratch; they are now permanent S15 assertions (a planted junction inside `.claude\` pointing at an outside dir, a `..\` entry aimed at a user file, both with hashes the hash gate alone would approve — the reach guards must refuse, and do). Selftest **171→182**, first-run green. `fable-expertise` plugin manifest bumped **0.1.0→0.2.0** to track its r30 content changes (nine cap-compliant descriptions, brief re-sync, coverage anchors). Exe rebuilt (**126,976 B**, sha256 `36290C17…07F3`), sandbox-verified end-to-end (INSTALL PASSED, ledger r32, VersionInfo clean), receipt appended; deployed src→live through the installer (v9 hooks live via the superseded-variant upgrade path, personal overlays preserved, regression gate 6/6). An independent fresh-eyes review of the combined r31+r32 diff ran before this commit — the review the r31 round had declared as its one process gap.
- 2026-07-18 r32-review — the independent fresh-eyes review of the r31+r32 diff (run before the r32 commit, per its own promise) returned PASS with two MAJOR execution-proven residuals in the new unknown-field walker, fixed inside the round: **camelCase and plural credential keys** (`authToken`, `clientSecret`, `apiKeys`) dodged `CRED_NAME`'s kebab/snake anchors — keys are now case-normalized and de-pluraled before testing; **credential-named parents of arrays and wrapper objects** leaked their leaves — credential context now propagates through the subtree (leaf-level `auth.mode`-class scheme keys stay exempt), and numeric leaves under a credential context redact. Also from the review: the attached `-p` password rule no longer mangles `-port=8080`/`-parallel=4`-style single-dash long flags or `-p8080:80` docker port maps (all execution-proven both directions); the classifier's `diagnos` stem narrowed to diagnosis-forms so "add diagnostics to the logger" stays silent while "differential diagnosis" fires; the ledger traversal refusal now logs loudly and S15 pins the guard's own message, not merely the outcome; and six S7 isolation fixtures carry values that are NOT independently secret-shaped, so the key-name branch fails alone if it breaks — the exact blind spot that let the camelCase gap survive r31. Selftest **175→182**.
- 2026-07-18 — **expertise-system quality & consistency audit** (working tree + live in lockstep; commit pending owner). Full re-audit executed green: coverage 24/24 on BOTH trees (4,996 terms), structure lint 25/25 both roots, repo↔live SHA256 parity 25/25, `claude plugin validate` EXIT=0 (intentional author warning), topic-map 63/63 valid owners, MANIFEST table figures re-verified current, backticked cross-refs 0 dangling (30 distinct tokens, all resolve). Three independent fable-tier fresh-eyes reviews — contract semantics (8-skill cross-cluster sample), routing/descriptions (all 25), safety boundaries (14 sensitive skills) — ALL PASS: every gate present/binding/framing-resistant, both safeguard charters single-hosted with zero sibling drift, zero over-assurance language, no fabricated references, descriptions faithful (no over-claims); IFRS 18 — the one dated in-text standard — retrieval-confirmed current (issued 2024, effective 2027-01-01). Fixes: `lint-structure.ps1` gains a `$foreign` allowlist acknowledging the four coexisting fable-kit skills (the r30 unknown-dir check had broken the documented live-root invocation — 4 false FAILs → ALL PASS both roots) + MANIFEST note; the one un-cross-referenced seam closed with mutual rates/FX routing (`economics-policy-geo` ⟷ `finance-value`, one line each side); atlas routing note aligned to `invest-research`'s actual stock/ETF/sector/theme/portfolio/macro surface. PLATFORM FINDING (decision reserved to owner): the session skills-list block carries a cumulative description budget (first-fit; bracketed 18,333–18,925 chars in the audit session) vs 24,820 chars total across the 29 live skills — the 7 alphabetically-last expertise skills (physics-mastery → sustainability-climate-esg) get NO trigger text in heavy sessions; the always-shown atlas description still routes them. Queued for one owner-gated description round: total-mass reduction, ai-agentic-systems classic-ML trigger nouns (CV/recommenders/speech/RL — the clearest under-trigger risk), AI-cluster description seams now resolved only in bodies.
- 2026-07-18 — **description round: budget fit + trigger repair** (working tree + live lockstep; commit pending owner). All 24 domain-skill descriptions rewritten materially shorter so the set fits the session skills-list budget the same-day audit discovered: total description mass across the 29 live skills cut 24,820 → 17,172 chars (−31%), 1,161 under the observed 18,333 budget floor — every skill now carries trigger text at session start instead of the alphabetical tail-7 losing theirs (true session-start rendering verifiable only in a fresh session — declared residual check). Atlas router description deliberately unchanged. Missing trigger nouns added while shrinking: classic ML (computer vision, NLP, speech, recommenders, forecasting, reinforcement learning) → ai-agentic-systems; game theory + mechanism/auction design → data-decision-science; RPA + intelligent automation + process mining → digital-enterprise-tech. All 25 descriptions now uniformly double-quoted (retires the strict-YAML description class permanently); 24 briefs' copy-EXACTLY blocks re-synced; plugin.json 0.2.0 → 0.3.0. Verified: lint 25/25 both roots, coverage 24/24 both trees, claude plugin validate EXIT=0, repo↔live SHA256 parity 25/25, per-desc max 692 ≤ 1024. Independent fable-tier fresh-eyes review of the full diff: PASS on all six lenses (lost-triggers / over-claims / additions / safety clauses / mechanics / voice), zero over-claims, safety clauses intact — its four medium lost-trigger findings (patent landscaping, model-risk, physics fluids/acoustics/CFD, medical affairs) and two dentistry safety-wording softenings (operative-decisions gate, "named" sources) all closed by +97-char micro-restorations, re-verified green. Sizing-method finding for future description edits: freehand length estimates ran 15–20% low on two consecutive passes; what converged was validate-before-write with a hard sum cap (14,400 for the 24). Accepted trade-off (reviewer-cataloged): ~30 low-severity noun drops, each covered by the skill's Use-clause catch-all, a sibling description, or atlas routing.
- 2026-07-18 — **live-currency pass on named standards & regulations** (as-of 2026-07-18; every figure below retrieved this session, none recalled). Swept all 25 SKILL.md files for named standards/regs (~120 hits), separated the retrieval-gated anchors (the overwhelming majority — "retrieve the current version", "edition retrieved when cited", "verify it is still current") from the handful stated as current fact, and verified the latter live. **Confirmed current, no edit needed:** CONSORT 2025 + SPIRIT 2025 (both published April 2025, 30- and 34-item checklists); NIST FIPS 203/204/205 PQC (finalized 2024-08-13, effective 2024-08-14); ISO/IEC 42001:2023 (2023-12-18) and ISO/IEC 23894:2023; NIST SP 800-218 SSDF v1.1 (current final; Rev.1/v1.2 still draft); ICH Q2(R2)+Q14 (adopted 2023-11-01, effective since June 2024); the 2017 World Workshop periodontal classification (no successor published); IFRS 18 (re-confirmed: issued 2024, effective 2027-01-01, replacing IAS 1). **Two fixes applied.** (1) `medicine-clinical-health` told readers to "use the E6(R3) Annex 2 lens" with no timing qualifier, but Annex 2 reached Step 4 only on 2026-06-03 (CHMP 2026-06-25) and does not come into effect until 2027-01-15, while the Principles/Annex 1 have been in force since 2025-07-23 — a real regulatory-timing defect in a clinical skill; the line now tells the reader to confirm in-force status in their region before treating Annex 2 as binding rather than directional (date-free so it cannot itself go stale). (2) `ai-agentic-systems` said to retrieve "the current consolidated text" of the EU AI Act — insufficient once an amending instrument exists separately: the Digital Omnibus (Parliament 2026-06-16, Council 2026-06-29, signed 2026-07-08) deferred Annex III standalone high-risk obligations from 2026-08-02 to 2027-12-02 and Annex I embedded high-risk from 2027-08-02 to 2028-08-02, while adding new prohibitions; both the evidence-set and risk-tiering lines now require the amending instruments in force alongside the base text, matching the amendment-awareness framing `sustainability-climate-esg` already carried. **Design validation — the headline result:** the two most volatile regulatory families in the system both moved materially in the weeks before this pass (EU AI Act via the Digital Omnibus; SEC climate rules proposed for full rescission 2026-06-03 with comment through 2026-08-03, CSRD narrowed and postponed by Feb-2026 legislation) and **not one skill went stale**, because none asserts a date it does not retrieve — `sustainability-climate-esg` had even pre-named the failure mode ("move under amendment and litigation"). Retrieval-gating is doing exactly the job it was designed for; the only defects found were where gating was absent (E6 Annex 2) or under-specified (base-text-only retrieval). Reported, not fixed (content additions, not currency corrections): NIST SP 800-218A (GenAI/dual-use SSDF profile) is unnamed in `software-engineering-mastery` despite its AI-engineering scope; ISO/IEC 42006 (AIMS certification-body requirements) unnamed in `ai-agentic-systems`; `chemistry-materials` names "ICH Q2" generically where Q2(R2) is current (its retrieve-gate covers this). Verified after the edits: lint 25/25 both roots, coverage 24/24 both trees, plugin validate EXIT=0, repo<->live parity 25/25, line counts unchanged (151/155) so MANIFEST stays accurate.
- 2026-07-18 — **currency-pass follow-through: SP 800-218A + ISO/IEC 42006 adopted** (the two reported-not-fixed items from r35, at owner request). Both verified live this session before being written into durable files, since a search-result title is not a source: **NIST SP 800-218A** — final, July 2024, "Secure Software Development Practices for Generative AI and Dual-Use Foundation Models: An SSDF Community Profile", augmenting SP 800-218 v1.1 for AI model development across the SDLC (the CSRC "(Withdrawn)" marker sits on the superseded initial public draft, not the final); **ISO/IEC 42006:2025** — published 2025-07-07, "Requirements for bodies providing audit and certification of artificial intelligence management systems", building on ISO/IEC 17021-1, aimed at certification bodies auditing against ISO/IEC 42001 and the accreditation bodies assessing them. Added as judgment, not nouns: `software-engineering-mastery` now routes generative-AI and foundation-model artifacts to the 800-218A profile and states that it layers model-development practice (training-data provenance, weights as release artifacts, pre-release evaluation) onto the SSDF spine rather than replacing it, with both versioning independently so both get retrieved; `ai-agentic-systems` adds 42006 to the evidence-set text list and, at the certification step, tells the reader to read a counterparty's AIMS certificate against 42006 plus a named accreditation (who audited, under what accreditation, against which edition) because an unaccredited certificate evidences less than it appears to. No dates written into skill text, per the r35 rule. Verified: lint 25/25 both roots, coverage 24/24 both trees, plugin validate EXIT=0, repo<->live parity 25/25, and line counts held at exactly 207/155 so the MANIFEST table stays accurate. `chemistry-materials`'s generic "ICH Q2" is deliberately left as-is — its adjacent retrieve-gate resolves to Q2(R2) at use time, and hardcoding the revision would create the staleness this pass exists to remove.
- 2026-07-18 — **consistency + tidy pass (docs only; no skill content touched).** Closing sweep after r33–r36: audited every version marker, reconciled stale doc claims, re-ran the full battery, tidied the container. **Version audit — everything already at its correct latest, nothing bumped for its own sake:** plugin `0.3.0` (r34); kit `r32` in `src/kit-manifest.json`, `docs/OVERVIEW.md`, and the last `build-receipts.jsonl` entry — all correct and deliberately NOT advanced, because `git diff 757e06d..e0fe840` shows r33–r36 touched only `expertise-system/` and `CHANGELOG.md`, leaving the kit payload `src/` untouched since r32, so the exe needs no rebuild; `EXE\FableSetup.exe` re-hashed and **matches its r32 build receipt** byte-for-byte (128,512 b, SHA-256 E25BB07C…). Kit live-vs-src drift check: `CLAUDE.md` and `fable-mode`/`organize`/`refresh-kit` all in sync; `invest-research` differs at exactly one line — the standing-profile line, where the kit ships a template and live carries the owner's real profile. That is the r19 PII-protection design working, **not** drift: syncing live→src there would publish personal data, so it is left alone (noted here so no future pass "fixes" it). **Stale claims corrected:** `expertise-system/MANIFEST.md` — plugin version 0.2.0→0.3.0, the "Open" section retitled "Status" with all three items resolved (currency now PASS with its findings, publishing now DONE), and a new Verified bullet recording the 2026-07-18 re-audit + four independent reviews; `expertise-system/README.md` — same version fix, verification header re-dated, publishing item marked done, a new currency item, the cumulative description-budget lesson added beside the per-description 1024-char cap, and the closing "focus on the open items" line rewritten now that none remain; repo `README.md` — the "Publishing (when you finalize)" heading now states published status while keeping the standing push rules intact; `docs/session-handoff-2026-07-16.md` — left byte-unedited below a SUPERSEDED banner, since falsifying a dated handoff would be worse than the staleness it warns about (it had read "No git remote; history predates the PII scrub", alarming and false today). **Hassle check:** the documented re-audit commands were executed verbatim as written, from the directory the docs assume — both returned `RESULT: ALL PASS`. **Final battery, all green:** lint 25/25 and coverage 24/24 on both roots, plugin validate EXIT=0, repo↔live parity 25/25, MANIFEST's 25 line counts each re-derived against the real files (0 mismatches), every internal doc link resolves, exe hash verified. **Tidy:** six consumed one-shot scripts cleared from scratch; three-folder container rule audited clean — `sharable\`/`private\`/`EXE\` plus `CLAUDE.md` and the harness-owned `.claude\`, zero strays, `EXE\` holding exactly one artifact.
